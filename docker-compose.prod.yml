services:
  api:
    image: ghcr.io/h-adji/braxes-api:prod
    build: .
    restart: always
    ports:
      - 127.0.0.1:3000:3000
    environment:
      SQLITE_DATA: /data
      IS_PROD: 1
    volumes:
      - db-data:/data
volumes:
  db-data:
